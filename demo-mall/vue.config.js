module.exports = {
  configureWebpack: {
    resolve: {
      //配置别名
      alias: {
        //"@": "src",
        //"assets": "@/assets",
        //"common": "@/components/common",
        //"components": "@/components",
        //"network": "network",
      }
    }
  }
}
